ktor {
    application {
        modules = [ "no.bekk.ApplicationKt.module" ]
    }
    deployment {
        port = 8080
        frontendUrlHost = "localhost:3000"
    }
    database {
        host = "localhost"
        port = 5432
        name = "kontrollere"
        user = "insert_username_here"
        password = "pwd"
    }
}